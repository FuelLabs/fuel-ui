import { createStitches } from '@fuel/theme'
export type { CSS } from '@stitches/react/types/css-util'

export const {
  styled,
  css,
  globalCss,
  keyframes,
  getCssText,
  theme,
  createTheme,
  config,
} = createStitches()
